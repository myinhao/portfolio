# 个人简历网站部署记录

日期：2026-03-25

## 部署状态

### 前端服务
- **服务类型**：Python HTTP Server
- **端口**：80
- **访问地址**：http://114.132.58.112

### 后端服务
- **服务类型**：Node.js API
- **端口**：3001
- **运行路径**：/root/portfolio-server

## 启动脚本 (start_server.sh)

```bash
#!/bin/bash

echo "=== 启动个人简历网站 ==="

# 检查前端服务是否已运行
FRONTEND_PID=$(ps aux | grep -E 'python3.*http.server' | grep -v grep | awk '{print $2}')
if [ ! -z "$FRONTEND_PID" ]; then
    echo "前端服务已在运行 (PID: $FRONTEND_PID)"
else
    echo "启动前端服务..."
    cd /home/简历/app/dist && python3 -m http.server 80 &
    FRONTEND_PID=$!
    echo "前端服务启动成功 (PID: $FRONTEND_PID)"
fi

# 检查后端服务是否已运行
BACKEND_PID=$(ps aux | grep -E 'node.*portfolio-server/server.js' | grep -v grep | awk '{print $2}')
if [ ! -z "$BACKEND_PID" ]; then
    echo "后端服务已在运行 (PID: $BACKEND_PID)"
else
    echo "启动后端服务..."
    cd /root/portfolio-server && /opt/mcsmanager/node-v20.12.2-linux-x64/bin/node server.js &
    BACKEND_PID=$!
    echo "后端服务启动成功 (PID: $BACKEND_PID)"
fi

echo ""
echo "=== 服务状态 ==="
echo "前端服务: http://服务器IP:80"
echo "后端API: http://服务器IP:3001"
echo ""
echo "启动完成！"
```

## 停止脚本 (stop_server.sh)

```bash
#!/bin/bash

echo "=== 停止个人简历网站 ==="

# 停止前端服务
FRONTEND_PID=$(ps aux | grep -E 'python3.*http.server' | grep -v grep | awk '{print $2}')
if [ ! -z "$FRONTEND_PID" ]; then
    echo "停止前端服务 (PID: $FRONTEND_PID)"
    kill $FRONTEND_PID
    echo "前端服务已停止"
else
    echo "前端服务未运行"
fi

# 停止后端服务
BACKEND_PID=$(ps aux | grep -E 'node.*portfolio-server/server.js' | grep -v grep | awk '{print $2}')
if [ ! -z "$BACKEND_PID" ]; then
    echo "停止后端服务 (PID: $BACKEND_PID)"
    kill $BACKEND_PID
    echo "后端服务已停止"
else
    echo "后端服务未运行"
fi

echo ""
echo "停止完成！"
```

## 使用说明

```bash
# 启动服务
./start_server.sh

# 停止服务  
./stop_server.sh
```

## 部署说明

- 由于系统是只读文件系统，无法安装nginx，使用Python HTTP Server作为临时解决方案
- 后端使用mc服务器管理器提供的Node.js环境运行
- 云服务器安全组已开放80端口，可通过公网访问
